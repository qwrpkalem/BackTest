# ---------------- 원천 데이터 캐시 ----------------
# 723MB / 5,039개 파일. src/fetch.py 와 src/prepare.py 로 언제든 재수집 가능하므로
# 저장소에는 올리지 않는다. (수집 방법은 backtest_spec.md §2-1 참고)
data/daum/
data/naver/
data/index/

# 파생 캐시 — 유니버스 필터 조건이 바뀌면 자동 재계산됨
data/universe.pkl

# 수집 로그
data/*.log

# ---------------- 파이썬 ----------------
__pycache__/
*.py[cod]

# ---------------- 로컬 실행 상태 ----------------
.claude/
